info:
  name: Premium Billing
  type: folder
  seq: 4

request:
  auth: inherit
  headers:
    - name: Cookie
      value: "{{cookie}}"

docs: |-
  Integration tests for premium and billing endpoints.
  Run Auth/Sign-in first when validating authenticated flows.
